<nav class="w-full fixed top-0 left-0 z-50 bg-[#f2f3f4] text-black shadow">
 

  <!-- NAVEGACIÓN -->
  <div
    class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between relative"
  >
    <!-- LOGO -->
    <a
      href="/"
      class="flex items-center gap-2 px-4 py-2 rounded-full border border-gray-200"
    >
      <img src="/img/bolsa.png" alt="Logo" class="h-6" />
      <span class="text-xl font-bold">DULCEBAG</span>
    </a>

    <!-- BOTÓN HAMBURGUESA -->
    <button id="menu-toggle" class="md:hidden text-[#343439]">
      <svg
        class="h-6 w-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>
    <!-- SCRIPT PROMO Y MENÚ -->
    
  </div>
</nav>
